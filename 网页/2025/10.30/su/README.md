本目录包含用于 `30.html` 的样式和脚本文件。

文件：
- styles.css: 焦点图样式（箭头、圆点、信息条）
- script.js: 自动轮播、左右按钮、点导航、悬停暂停、键盘支持

预览：在 Windows 上直接用浏览器打开 `30.html` 即可（双击或右键在浏览器中打开）。

说明：
- 当前示例使用 `hu.png` 六次作为占位图，替换 `su/hu.png` 为不同图片即可。
- 如果部署在服务器，请确保 `su/` 文件夹和图片路径保持相对关系。
